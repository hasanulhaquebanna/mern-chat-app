import { Box } from "@chakra-ui/react";
import React from "react";
import SideBar from "./SideBar";

const Home = () => {
  return (
    <Box>
      <SideBar />
    </Box>
  );
};

export default Home;
